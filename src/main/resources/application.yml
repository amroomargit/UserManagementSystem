server:
  port: 8081

spring:
  application:
    name: UserManagementSystem

  datasource:
    url: jdbc:postgresql://localhost:5432/studentdb
    username: postgres
    password: QaZ123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # this line allows Spring Data JPA to automatically create your tables based on your @Entity
      #classes. INSTEAD of coding it manually in DataGrip using traditional SQL commands.
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
